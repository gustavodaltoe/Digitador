import { render, screen } from '@testing-library/react';

import Header from '.';

describe('<Header />', () => {
  it('should describe here', () => {
    const { container } = render(<Header />);
  });
});
